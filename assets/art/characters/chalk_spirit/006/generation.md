# 粉笔精灵混合动画资产 006

本版本是获批候选 `design/concepts/chalk-spirit/chalk-rig/003` 的自包含正式资产快照。

- 骨骼拆件由已批准序列帧资产 005 的待机帧程序化分层得到，未重新生成角色主体。
- 待机、移动、远程、施法、受击与濒危使用刚性拆件补间；退场保留资产 005 的序列帧。
- 三张序列帧图集及帧元数据随版本保存，避免后续替换 005 时破坏退场回退。
- 粉尘拆件供独立粒子层与施法法阵使用；弹体仍使用独立的 `chalk_projectile` 正式对象。
- 远程攻击主体后坐方向与弹体方向相反；施法使用青紫双环法阵。

具体动作实现、时序与后端选择以 Godot 正式表现场景和主动画资源为准，不在 Manifest 重复保存。
